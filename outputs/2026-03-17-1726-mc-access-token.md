> **Question:** Enter your access token to unlock Mission Control.

This looks like a Mission Control UI prompt that was sent to me rather than typed into the MC interface.

**Compass does not hold or manage access tokens.** I'm a strategic advisor — authentication credentials are outside my scope.

If you're trying to log into Mission Control:
- The MC UI is at `http://<host>:3000` (or wherever OpenClaw is running)
- The access token is set in `/home/pi/openclaw/mission-control/.env` — look for `ACCESS_TOKEN` or similar
- If you've forgotten it, check that `.env` file directly

If you meant to ask me something else, please rephrase and I'll help.
